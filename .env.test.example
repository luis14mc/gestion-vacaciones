# =============================================================
# Variables de Entorno - Tests de integración (LOCAL)
# =============================================================
# Copiar como .env.test (ese archivo NO se sube a Git):
#   cp .env.test.example .env.test
#
# Neon: solo para desarrollo local. Pega tu URL en .env.test;
# nunca la commits. Ver docs/ARCHIVOS_NO_VERSIONADOS.md
# =============================================================

DATABASE_URL=postgresql://testuser:testpass@localhost:5432/vacaciones_test
DATABASE_URL_TEST=postgresql://testuser:testpass@localhost:5432/vacaciones_test
NODE_ENV=test
AUTH_SECRET=test-secret-key-for-integration-tests
NEXTAUTH_URL=http://localhost:3000
